[project]
name = "codelab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-adk>=0.3.0",
    "langchain>=0.3.24",
    "toolbox-langchain>=0.1.0",
]

[tool.uv.workspace]
members = ["hotel-agent-app"]
